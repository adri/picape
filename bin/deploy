#!/usr/bin/env bash
#
# Deploy Script
#
bash bin/update

cd assets/ && {
  yarn build;
  cd -;
}

mkdir -p priv/static/
MIX_ENV=prod mix phx.digest
MIX_ENV=prod mix compile

pkill phoenix.server
MIX_ENV=prod elixir --detached --sname picape -S mix phoenix.server

echo "Finished! Deployed."
