{
  "name": "picape-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
