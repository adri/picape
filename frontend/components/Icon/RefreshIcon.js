import * as React from 'react';

import { CircleIcon } from './CircleIcon';

export function RefreshIcon(props) {
  return <CircleIcon name="refresh" glyphSize={18} accessibilityLabel="Vervangen" {...props} />;
}
